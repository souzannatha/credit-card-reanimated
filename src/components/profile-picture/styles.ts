import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginTop: 60,
  },
  imageBorder: {
    borderWidth: 2,
    borderColor: "#373843",
    borderRadius: 100,
    padding: 8,
  },
  image: {
    width: 90,
    height: 90,
  },
  welcome: {
    marginTop: 30,
    fontSize: 24,
    fontWeight: "400",
    color: "#fff",
  },
});
